import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { ImageReveal, QuoteForm, SectionLabel } from "@/components/luxury-site";
import { services } from "@/lib/site-data";
import { Button } from "@/components/ui/button";
export const Route = createFileRoute("/services/$slug")({ loader: ({ params }) => { const service = services.find((s) => s.slug === params.slug); if (!service) throw notFound(); return service; }, head: ({ loaderData }) => ({ meta: [{ title: `${loaderData?.title ?? "Service"} | Creative Granite & Design` }, { name: "description", content: loaderData?.copy ?? "Creative Granite & Design service details." }, { property: "og:title", content: `${loaderData?.title ?? "Service"} | Creative Granite & Design` }, { property: "og:description", content: loaderData?.copy ?? "Creative Granite & Design service details." }] }), component: ServicePage, notFoundComponent: () => <div className="px-5 py-40"><h1 className="font-display text-6xl">Service not found</h1><Link to="/services">Back to services</Link></div> });
function ServicePage() { const service = Route.useLoaderData(); return <><section className="grid min-h-[82vh] bg-obsidian text-ivory md:grid-cols-2"><div className="px-5 pb-16 pt-40 md:px-10"><SectionLabel>Creative Granite Service</SectionLabel><h1 className="font-display text-7xl leading-none md:text-9xl">{service.title}</h1><p className="mt-8 max-w-xl text-lg text-ivory/70">{service.copy}</p><Button className="mt-8" variant="bronze" asChild><Link to="/contact">Get Estimate Now</Link></Button></div><ImageReveal src={service.image} alt={service.title} className="min-h-[520px]" /></section><section className="px-5 py-24 md:px-10"><div className="mx-auto grid max-w-7xl gap-12 md:grid-cols-3"><article><SectionLabel>Benefits</SectionLabel><p className="text-muted-foreground">Durable materials, tailored layouts, precise fabrication, and a refined installation experience.</p></article><article><SectionLabel>Process</SectionLabel><p className="text-muted-foreground">Free estimate, material selection, templating, fabrication, installation, and final detail review.</p></article><article><SectionLabel>Related Services</SectionLabel><div className="space-y-2">{services.slice(0,4).map((s) => <Link key={s.slug} to="/services/$slug" params={{ slug: s.slug }} className="block text-bronze hover:underline">{s.title}</Link>)}</div></article></div></section><section className="bg-card px-5 py-24 md:px-10"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-2"><h2 className="font-display text-6xl">Start Your {service.title} Project</h2><QuoteForm /></div></section></>; }
